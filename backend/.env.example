# ── 数据库 ──────────────────────────────────────────────
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/echomind

# ── 安全 ────────────────────────────────────────────────
# 生产环境必须设置！留空则每次重启自动生成随机密钥（仅限开发）
SECRET_KEY=
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ── LLM 配置 ───────────────────────────────────────────
LLM_PROVIDER=gemini
LLM_API_KEY=your-api-key-here
LLM_MODEL=gemini-2.0-flash
LLM_MAX_TOKENS=1024
LLM_TEMPERATURE=0.7
# HTTP 代理（可选，留空则直连）
LLM_PROXY=
