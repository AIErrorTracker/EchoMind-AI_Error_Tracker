import 'package:flutter/material.dart';

class Step5ConceptTestWidget extends StatelessWidget {
  const Step5ConceptTestWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
