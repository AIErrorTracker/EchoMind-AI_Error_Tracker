import 'package:flutter/material.dart';

class Step2DecisionTrainingWidget extends StatelessWidget {
  const Step2DecisionTrainingWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
