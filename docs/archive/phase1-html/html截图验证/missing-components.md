# Missing Components Report

## 组件目录存在但未运行时挂载（待清理/待接入）

### upload-history
- `history-timeline`

## 运行时组件截图缺失（未捕获到可见节点）

无。

## 采集错误明细

### community
- `default` / `board-feature-boost`: 候选节点存在但当前不可见
- `default` / `board-feedback`: 候选节点存在但当前不可见
